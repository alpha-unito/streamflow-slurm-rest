version: v1.0

workflows:
  hello-slurm:
    type: cwl
    config:
      file: main.cwl
    bindings:
      - step: /
        target:
          deployment: slurm-deployment
          service: test-service

deployments:
  slurm-deployment:
    type: unito.slurmrest
    workdir: /path/to/workdir
    config:
      api_address: <slurm-api-address>
      jwt_token: "<JWT_TOKEN>" # must start with either "file:" or "env:" or be the token itself
      services:
        test-service:
          partition: <partition-name>