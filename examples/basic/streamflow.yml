version: v1.0

workflows:
  hello-slurm:
    type: cwl
    config:
      file: main.cwl
    bindings:
      - step: /
        target:
          deployment: slurm
          service: broadwell

deployments:
  c3sssh:
    type: ssh
    workdir: /beegfs/home/tommaso.fogliobonda/streamflow
    config:
      nodes:
        - c3sfr1.di.unito.it
      username: tommaso.fogliobonda
      sshKey: ~/.ssh/unito/hpc
      checkHostKey: false

  slurm:
    type: unito.slurmrest
    config:
      api_address: "https://api.slurm.hpc4ai.unito.it"
      jwt_token: "/home/tommo/Desktop/Coding/Alpha/streamflow-slurm/streamflow-slurm-rest/examples/jwt"
      services:
        broadwell:
          partition: "broadwell"
    wraps: c3sssh